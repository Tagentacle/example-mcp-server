[package]
name = "example_mcp_server"
version = "0.2.0"
description = "Example MCP Server node providing weather tools via Streamable HTTP"
authors = ["Tagentacle Contributors"]
type = "executable"

[entry_points]
node = "server:main"

[dependencies]
python = ["tagentacle-py-mcp"]
